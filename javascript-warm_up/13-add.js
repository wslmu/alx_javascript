#!/usr/bin/node
//a function that returns the addition of 2 integers

exports.add = (a, b) => a + b

// can also be written as
// function add(a, b) {
//     return a + b;
//   }

// console.log(add(3, 5));